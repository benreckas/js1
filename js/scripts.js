function logMessage() {
  console.log('Clicked!');
};

// Anytime you have code in JS, if it's the end of the execution context,
// end it with a semi-colon.

// Looking for correct camel casing and semi-colon use.

// Comments are not optional. You MUST DO IT.

/*
  This is a multi-line
  comment
*/

var name = "Ben";

// ES5 String Interpolation
var message = "Hello there, " + name + "!";
console.log(message);

// ES6 String Interpolation
var secondMessage = `Hello there, ${name}!`;
console.log(secondMessage);

// If you can write less code and it still works the same- do that.


// Variable declaration

console.log(number);

var number = 14;

// Variable Definition
